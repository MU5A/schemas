/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/resources/{resourceType}/{resourceId}/share": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Grant or revoke access to a resource
         * @description Grants access to the actors listed in grantAccess, revokes access from the actors listed in revokeAccess, and optionally notifies the affected actors. Both lists are carried on a single request so a caller can grant and revoke in the same call; either list may be empty, but the field itself is always present, matching the only known-correct client (see grantAccess/revokeAccess below).
         *
         *     This one operation spans three different resource types, chosen at call time by resourceType, so the generated RTK Query client cannot auto-invalidate the affected design, filter, or view list cache the way a single-resource-type operation would: RTK Query cache tags are static per operation, derived from this operation's own OpenAPI tag, not from a runtime path parameter. A caller of the generated useShareResourceMutation hook must invalidate or refetch the relevant resource list itself after a successful share; the mutation alone will not refresh it.
         */
        post: operations["shareResource"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description One grant or revoke target in a ResourceAccessMappingPayload. */
        Actor: {
            /**
             * Format: uuid
             * @description A Universally Unique Identifier used to uniquely identify entities in Meshery. The UUID core definition is used across different schemas.
             */
            actorId: string;
            /**
             * @description Kind of actor. Every known client sends user; the server field is a plain string with no enforced enum, so this is modelled as an open string rather than an enum of one value that would reject a legitimate future actor type (for example a team or a service account) before the server itself does.
             * @example user
             */
            actorType: string;
        };
        /** @description Request body for POST /api/resources/{resourceType}/{resourceId}/share. Every field is required because the only confirmed-correct client always sends all three; a request missing one has not been verified against the server's actual decode behavior for a missing key, only for a differently-named one, which the server accepts and silently drops (see the parent issue this construct was added for). */
        ResourceAccessMappingPayload: {
            /** @description Actors to grant access to. May be empty. */
            grantAccess: {
                /**
                 * Format: uuid
                 * @description A Universally Unique Identifier used to uniquely identify entities in Meshery. The UUID core definition is used across different schemas.
                 */
                actorId: string;
                /**
                 * @description Kind of actor. Every known client sends user; the server field is a plain string with no enforced enum, so this is modelled as an open string rather than an enum of one value that would reject a legitimate future actor type (for example a team or a service account) before the server itself does.
                 * @example user
                 */
                actorType: string;
            }[];
            /** @description Actors to revoke access from. May be empty. */
            revokeAccess: {
                /**
                 * Format: uuid
                 * @description A Universally Unique Identifier used to uniquely identify entities in Meshery. The UUID core definition is used across different schemas.
                 */
                actorId: string;
                /**
                 * @description Kind of actor. Every known client sends user; the server field is a plain string with no enforced enum, so this is modelled as an open string rather than an enum of one value that would reject a legitimate future actor type (for example a team or a service account) before the server itself does.
                 * @example user
                 */
                actorType: string;
            }[];
            /** @description Whether to notify the affected actors of the change. */
            notifyUsers: boolean;
        };
    };
    responses: {
        /** @description Invalid request body or request param */
        400: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "text/plain": string;
            };
        };
        /** @description Expired JWT token used or insufficient privilege */
        401: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "text/plain": string;
            };
        };
        /** @description Result not found */
        404: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "text/plain": string;
            };
        };
        /** @description Internal server error */
        500: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "text/plain": string;
            };
        };
    };
    parameters: {
        /** @description Kind of resource being shared. Known values reported against the server route as of this writing: pattern (the wire name for a design), filter, and view. This is not modelled as an enum: the server route accepts the segment as a plain string, and the full set of resource kinds it recognizes has not been independently confirmed against the server source, only against the one confirmed-correct client (layer5io/sistent's ShareModal, which maps its own dataName prop straight onto this segment, translating only design to pattern). */
        resourceType: string;
        /** @description Identifier of the resource being shared. */
        resourceId: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    shareResource: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Kind of resource being shared. Known values reported against the server route as of this writing: pattern (the wire name for a design), filter, and view. This is not modelled as an enum: the server route accepts the segment as a plain string, and the full set of resource kinds it recognizes has not been independently confirmed against the server source, only against the one confirmed-correct client (layer5io/sistent's ShareModal, which maps its own dataName prop straight onto this segment, translating only design to pattern). */
                resourceType: string;
                /** @description Identifier of the resource being shared. */
                resourceId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Actors to grant access to. May be empty. */
                    grantAccess: {
                        /**
                         * Format: uuid
                         * @description A Universally Unique Identifier used to uniquely identify entities in Meshery. The UUID core definition is used across different schemas.
                         */
                        actorId: string;
                        /**
                         * @description Kind of actor. Every known client sends user; the server field is a plain string with no enforced enum, so this is modelled as an open string rather than an enum of one value that would reject a legitimate future actor type (for example a team or a service account) before the server itself does.
                         * @example user
                         */
                        actorType: string;
                    }[];
                    /** @description Actors to revoke access from. May be empty. */
                    revokeAccess: {
                        /**
                         * Format: uuid
                         * @description A Universally Unique Identifier used to uniquely identify entities in Meshery. The UUID core definition is used across different schemas.
                         */
                        actorId: string;
                        /**
                         * @description Kind of actor. Every known client sends user; the server field is a plain string with no enforced enum, so this is modelled as an open string rather than an enum of one value that would reject a legitimate future actor type (for example a team or a service account) before the server itself does.
                         * @example user
                         */
                        actorType: string;
                    }[];
                    /** @description Whether to notify the affected actors of the change. */
                    notifyUsers: boolean;
                };
            };
        };
        responses: {
            /** @description Access mapping updated */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description Invalid request body or request param */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/plain": string;
                };
            };
            /** @description Expired JWT token used or insufficient privilege */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/plain": string;
                };
            };
            /** @description Result not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/plain": string;
                };
            };
            /** @description Internal server error */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/plain": string;
                };
            };
        };
    };
}
